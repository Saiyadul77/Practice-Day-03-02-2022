const ages = [10, 15, 20, 7, 25, 33, 32, 40, 55, 90, 100, 57, 60, 47, 35];
/* for (let i = 0; i < ages.length; i++) {
    const element = ages[i];
    console.log(element);
} */
for (const element of ages) {
    console.log(element);
}
